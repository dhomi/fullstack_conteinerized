apiVersion: v1
kind: Service
metadata:
  name: {{ include "techhelm.fullname" . }}-grafana
  labels:
    io.kompose.service: grafana
  {{- include "techhelm.labels" . | nindent 4 }}
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (cbf2835db)
spec:
  type: {{ .Values.grafana.type }}
  selector:
    io.kompose.service: grafana
  {{- include "techhelm.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.grafana.ports | toYaml | nindent 2 -}}