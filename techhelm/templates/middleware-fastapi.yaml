apiVersion: v1
kind: Service
metadata:
  name: {{ include "techhelm.fullname" . }}-middleware-fastapi
  labels:
    io.kompose.service: middleware-fastapi
  {{- include "techhelm.labels" . | nindent 4 }}
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (cbf2835db)
spec:
  type: {{ .Values.middlewareFastapi.type }}
  selector:
    io.kompose.service: middleware-fastapi
  {{- include "techhelm.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.middlewareFastapi.ports | toYaml | nindent 2 -}}