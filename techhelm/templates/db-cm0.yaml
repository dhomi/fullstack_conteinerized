apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "techhelm.fullname" . }}-db-cm0
  labels:
    io.kompose.service: db
  {{- include "techhelm.labels" . | nindent 4 }}
data:
  init.sql: {{ .Values.dbCm0.initSql | quote }}